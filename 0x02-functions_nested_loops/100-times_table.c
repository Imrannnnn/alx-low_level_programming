#include "main.h"

/**
 * print_times_table - print the n times table
 * @n: the number
 * Return: alwasy 0
 */

void print_times_table(int n)
{
	if (n > 15)
	{
	else if	(n < 0)

		_putchar('\n');
	return (0);
	}
}
